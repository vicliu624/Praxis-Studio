import type { DevelopmentGraph } from "@praxis/development-graph";
export interface LocalKnowledgePaths { root: string; distinctionDir: string; graphDir: string; memoryDir: string; rulesDir: string; reportsDir: string; }
export function getLocalKnowledgePaths(projectRoot: string): LocalKnowledgePaths { const distinctionDir = `${projectRoot}/.distinction`; return { root: projectRoot, distinctionDir, graphDir: `${distinctionDir}/graph`, memoryDir: `${distinctionDir}/memory`, rulesDir: `${distinctionDir}/rules`, reportsDir: `${distinctionDir}/reports` }; }
export function createInitialGraph(): DevelopmentGraph { return { id: "graph:default", title: "Praxis Studio Project", updatedAt: new Date().toISOString(), nodes: [ { id: "node:intent", kind: "product_intent", title: "Product Intent", description: "The initial product idea.", status: "draft", progress: 0.1 }, { id: "node:architecture-memory", kind: "architecture_component", title: "Architecture Memory", description: "Records architecture decisions and development memory.", status: "wip", progress: 0.42 }, { id: "node:local-knowledge", kind: "architecture_component", title: "Local Knowledge", description: ".distinction project memory store.", status: "wip", progress: 0.64 } ], edges: [ { id: "edge:records", source: "node:architecture-memory", target: "node:local-knowledge", kind: "records", title: "records", description: "Architecture Memory records decisions into Local Knowledge.", status: "wip", progress: 0.4, riskLevel: "medium", blockedReason: "Memory event schema and stale detection are not fully defined." } ] }; }
